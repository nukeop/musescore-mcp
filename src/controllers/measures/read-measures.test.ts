import { describe, test } from "bun:test";

describe("read_measures", () => {
	test.todo("prints notes, rests and dotted durations as bar notation");
	test.todo("returns bars in range given by from/to");
	test.todo("errors for a file that does not exist");
	test.todo("errors when the range is outside the score");
});
